# Not a structured artifact

This file declares itself as an ADR log but contains no `## ADR-NNNN` headings
and no numbered principle blocks. There is nothing here the deterministic
adapter can parse into an atom — just prose.
