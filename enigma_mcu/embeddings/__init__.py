"""Embeddings layer — the `Embedder` port (SPEC-003) and, later, the real bge-m3
backend via Ollama (SPEC-004). SPEC-003 ships the port and a deterministic
`FakeEmbedder` only; no model, no network.
"""
